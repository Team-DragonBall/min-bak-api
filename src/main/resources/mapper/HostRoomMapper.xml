<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.minbak.web.host_room.HostRoomMapper">
<!--    숙소 정보 저장-->
    <insert id="insertHostRoom" parameterType="com.minbak.web.host_room.HostRoomDTO">
        INSERT INTO rooms (name, title, address, price, content, user_id)
        VALUES (#{name}, #{title}, #{address}, #{price}, #{content}, #{userId})
    </insert>
<!--    등록된 숙소 조회-->
    <select id="selectHostRoomById" parameterType="int" resultType="com.minbak.web.host_room.HostRoomDTO">
        SELECT room_id, name, address, price, content AS description FROM rooms WHERE room_id = #{roomId}
    </select>
</mapper>